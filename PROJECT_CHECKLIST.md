# ✅ PROJECT CHECKLIST — Rubric Audit

> Generated by auditing every file, folder, SQL definition, and live DB state against the Information Management Final Project rubric.
> Audit date: **April 2026** — ~2 weeks before the May 4–7 deadline.

**Legend:**
`✅ DONE` — fully implemented and working
`⚠️ PARTIAL` — started but incomplete or has gaps
`❌ MISSING` — not yet implemented

---

## SECTION 1 — FUNCTIONALITY & IMPLEMENTATION (30%)

### 1.1 CRUD Operations

| Check | Status | Notes |
|---|---|---|
| CREATE (INSERT) for all major tables | ✅ DONE | Every writable table has an INSERT path in its model |
| READ (SELECT) displays data correctly | ✅ DONE | All pages render data; `Database->read()` routes to slave |
| UPDATE for all major tables | ⚠️ PARTIAL | See breakdown below — 4 tables only get "soft" updates (lock/active flag flips) |
| DELETE for all major tables | ⚠️ PARTIAL | Only Delivery/Sales/Expenses have hard DELETE. Rest use soft-delete via `Active_status` / `Locked_status`. This is intentional for audit integrity but the rubric asks literally for DELETE |
| At least FOUR interconnected tables with CRUD | ✅ DONE | Delivery, Sales, Expenses, Inventory_Snapshot all interconnect via User + Kiosk + Product |

**Per-table CRUD matrix:**

| Table | CREATE | READ | UPDATE | DELETE | Notes |
|---|---|---|---|---|---|
| User | ✅ | ✅ | ✅ (active toggle) | ⚠️ soft-only | No hard delete — preserves audit chain |
| Role | ❌ seed only | ✅ | ❌ | ❌ | Reference table, never edited |
| Kiosk | ✅ | ✅ | ✅ | ⚠️ soft-only | Edits name/location/active |
| Category | ❌ seed only | ✅ | ❌ | ❌ | Reference table |
| Product | ✅ | ✅ | ✅ | ⚠️ soft-only | Full edit + photo upload |
| Inventory_Snapshot | ✅ | ✅ | ✅ (lock/qty) | ❌ | Append-only by design (audit integrity) |
| Delivery | ✅ | ✅ | ✅ (lock) | ✅ | Hard delete exists in model |
| Sales | ✅ | ✅ | ✅ (lock) | ✅ | Hard delete exists in model |
| Expenses | ✅ | ✅ | ✅ (lock) | ✅ | Hard delete exists in model |
| Audit_Log | ✅ | ✅ | ❌ | ❌ | Intentionally append-only |
| Time_in | ✅ | ✅ | ❌ | ❌ | Append-only clock-in |

**⚠️ What's missing:** Several tables have no UI-exposed DELETE. The codebase follows a "soft delete" philosophy (flip `Active_status = 0`) which is the *right* design for an audit-focused system but could be flagged by a literal-reading grader. **Recommendation:** add admin-only hard-delete methods on Category, Role, User (currently soft-only) so the rubric is satisfied on paper — but keep them gated behind role checks and Audit_Log entries.

---

### 1.2 Master-Slave Architecture

| Check | Status | Notes |
|---|---|---|
| Master configured on port 3306 | ✅ DONE | `C:/xampp/mysql/bin/my.ini` — `server-id=1`, `log-bin=mysql-bin` |
| Slave configured on port 3307 | ✅ DONE | `C:/xampp/mysql_slave/bin/my.ini` — `server-id=2`, `relay-log=mysql-relay-bin` |
| Writes route to Master | ✅ DONE | 27 `db->write()`/`db->insert()` calls, 0 direct PDO in models |
| Reads route to Slave | ✅ DONE | 41 `db->read()`/`db->readOne()` calls in models |
| Real-time sync working | ✅ DONE | Live check: `Slave_IO_Running=Yes`, `Slave_SQL_Running=Yes`, `Seconds_Behind_Master=0` |
| `Database.php` routes correctly | ✅ DONE | Centralized in `core/Database.php`; smoke-tested this session |

---

### 1.3 SQL Triggers for Change Logging

| Check | Status | Notes |
|---|---|---|
| AFTER INSERT trigger logs new entries | ✅ DONE | 7 `trg_log_<table>_insert` triggers — one per major table |
| AFTER UPDATE trigger logs OLD and NEW | ✅ DONE | 7 `trg_log_<table>_update` triggers capture both OLD and NEW as JSON |
| AFTER DELETE trigger logs deletes | ✅ DONE | 7 `trg_log_<table>_delete` triggers capture OLD as JSON |
| Log table exists | ✅ DONE | `Audit_Log` table present, now expanded |
| Log captures operation type | ✅ DONE | Dedicated `Operation` column (INSERT/UPDATE/DELETE) |
| Log captures affected table | ✅ DONE | Dedicated `Table_name` column |
| Log captures data changes (old + new) | ✅ DONE | `Old_values` and `New_values` TEXT columns holding JSON_OBJECT snapshots |
| Log captures timestamp | ✅ DONE | `Timestamp` column with `DEFAULT CURRENT_TIMESTAMP` |
| `SHOW TRIGGERS` shows active triggers | ✅ DONE | **28 triggers** confirmed active on Master (7 original + 21 new change-logging) |

**Current 7 triggers (live on Master):**

| Trigger | Event | Table | Purpose |
|---|---|---|---|
| `trg_calc_line_total_insert` | BEFORE INSERT | sales | Calculation (not logging) |
| `trg_calc_line_total_update` | BEFORE UPDATE | sales | Calculation |
| `trg_prevent_locked_sales_edit` | BEFORE UPDATE | sales | Validation |
| `trg_prevent_locked_inventory_edit` | BEFORE UPDATE | inventory_snapshot | Validation |
| `trg_prevent_locked_delivery_edit` | BEFORE UPDATE | delivery | Validation |
| `trg_prevent_locked_expense_edit` | BEFORE UPDATE | expenses | Validation |
| `trg_audit_inventory_unlock` | AFTER UPDATE | inventory_snapshot | Logging (unlock only) |

**✅ RESOLVED (2026-04-20):**
Expanded `Audit_Log` with `Operation`, `Table_name`, `Old_values`, `New_values` columns and added 21 `trg_log_*` triggers (AFTER INSERT/UPDATE/DELETE × 7 tables: Sales, Inventory_Snapshot, Delivery, Expenses, Product, User, Kiosk). Password column is deliberately excluded from User triggers. Verified with live insert/update/delete test on Product — Audit_Log rows 99, 100, 101 captured correct Operation / Table_name / JSON Old+New values. See `sql/migrations/2026_add_change_logging.sql`.

---

### 1.4 SQL Queries

**Basic:**

| Check | Status | Notes |
|---|---|---|
| SELECT used | ✅ DONE | 41+ SELECT statements |
| INSERT used | ✅ DONE | Every writable model |
| UPDATE used | ✅ DONE | 14+ UPDATE statements |
| DELETE used | ✅ DONE | Delivery/Sales/Expenses |

**Advanced:**

| Check | Status | Notes |
|---|---|---|
| JOIN used (2+ tables) | ✅ DONE | 36 JOIN instances — Product, Category, User, Kiosk all joined |
| GROUP BY used | ✅ DONE | 12 instances — summary reports use it |
| ORDER BY used | ✅ DONE | 27 instances |
| Subqueries used | ✅ DONE | `ReportModel::getProductsAboveAverageSales()` (nested derived tables in HAVING) and `ReportModel::getKiosksWithoutEndingSnapshot()` (NOT IN subquery) |
| Aggregate functions | ✅ DONE | COUNT, SUM, MAX heavily used (20+ spots) |

**✅ RESOLVED (2026-04-20):** Two subqueries added to `ReportModel.php` — one uses correlated-free derived tables in a HAVING clause ("products whose total quantity sold > average per-product total in the date range"), the other uses a `NOT IN` subquery ("active kiosks that have not yet recorded an ending snapshot for the given date"). Both tested live against the slave.

---

## SECTION 2 — DATABASE DESIGN & ARCHITECTURE (35%)

### 2.1 ERD / Schema

| Check | Status | Notes |
|---|---|---|
| ERD or relational schema documented | ⚠️ PARTIAL | `DATABASE_FULL_DOCUMENTATION.md` has a full written schema, but **no visual ERD diagram** exists (the original draw.io was the old pre-merge design) |
| ERD matches current system | ❌ MISSING | No updated visual. The old draw.io shows pre-merge state (separate Sales_Item, Delivery_Item tables) |
| All 11 tables shown | ⚠️ PARTIAL | Documented in text in `DATABASE_FULL_DOCUMENTATION.md`, but not in a diagram |

**Recommendation:** regenerate the ERD in draw.io / dbdiagram.io / MySQL Workbench reverse-engineer. Should take ~30 min. Export as PNG + SVG into a `docs/` folder.

---

### 2.2 Table Relationships

| Check | Status | Notes |
|---|---|---|
| 1:1 relationships documented | ⚠️ PARTIAL | None exist in this system (intentional — no 1:1 was needed). Document this fact explicitly |
| 1:M relationships documented | ✅ DONE | Kiosk→User, User→Sales, Product→Sales, etc. — all covered in `DATABASE_FULL_DOCUMENTATION.md` |
| M:N relationships noted | ⚠️ PARTIAL | Originally there would have been Sales–Product and Delivery–Product M:N, but these were merged into single tables (Sales and Delivery already have the link). Document that design decision |
| FK constraints in schema.sql | ✅ DONE | 17 FK constraints across 10 tables |

---

### 2.3 Master-Slave Design

| Check | Status | Notes |
|---|---|---|
| Master-slave architecture documented | ✅ DONE | `PROJECT_CONTEXT.md` §11 + `DATABASE_FULL_DOCUMENTATION.md` Part 4 + `PROJECT_STRUCTURE_EXPLAINED.md` §C |
| my.ini shows master setup | ✅ DONE | `server-id=1`, `log-bin=mysql-bin` in `C:/xampp/mysql/bin/my.ini` |
| Slave config shows server-id=2 | ✅ DONE | Confirmed in `C:/xampp/mysql_slave/bin/my.ini` |
| Slave shows `read_only=1` | ❌ MISSING | Grep of slave my.ini found no `read_only` directive. Slave is not enforcing read-only at the DB level — only the application layer prevents writes |

**Recommendation:** add `read_only=1` to slave `my.ini` and restart slave. 1-line fix.

---

### 2.4 Log Table Design

| Check | Status | Notes |
|---|---|---|
| Audit_Log / log table exists | ✅ DONE | `Audit_Log` table in schema |
| Captures operation type | ⚠️ PARTIAL | `Action VARCHAR(100)` — free text, not ENUM; works but not strict |
| Captures affected table name | ❌ MISSING | Not a separate column. Sometimes mentioned inside `Details` |
| Captures data changes (old + new) | ❌ MISSING | No `old_values` / `new_values` columns. `Details` holds arbitrary text |
| Captures timestamp | ✅ DONE | `Timestamp DATETIME DEFAULT CURRENT_TIMESTAMP` |
| Captures user who performed action | ✅ DONE | `User_ID INT` FK to User |

**Recommendation:** migrate to an expanded log schema (see §1.3 above). This is tied to the trigger gap — both should be fixed together.

---

## SECTION 3 — CODE QUALITY & ORGANIZATION (15%)

### 3.1 Project Structure

| Check | Status | Notes |
|---|---|---|
| MVC folder structure followed | ✅ DONE | `models/`, `controllers/`, `views/` all present and organized |
| `config/` with database.php + constants.php | ✅ DONE | Both files present |
| `core/` with Database, Model, Controller, Auth, Router | ✅ DONE | All 5 files present |
| `assets/` with css, js, img | ✅ DONE | All 3 subfolders present |
| `sql/` with schema.sql + triggers.sql | ✅ DONE | Plus `migrations/` and `backups/` subfolders |
| `index.php` entry point | ✅ DONE | Front controller in place |

---

### 3.2 Code Standards

| Check | Status | Notes |
|---|---|---|
| All classes use OOP | ✅ DONE | Every `.php` file except `index.php` is class-based |
| All SQL lives in models only | ✅ DONE | 0 raw SQL strings found in controllers/ or views/ |
| Prepared statements (no concat) | ✅ DONE | All queries use `?` placeholders; audited during smoke test |
| Role-based auth on every controller method | ✅ DONE | AdminController: 28 auth-calls / 12 methods, Sales/Delivery/Expense: 17 / 6, etc. Every method has at least one auth check |
| Passwords stored as bcrypt hashes | ✅ DONE | `UserModel::create` uses `password_hash($p, PASSWORD_BCRYPT)`; `AuthController::login` uses `password_verify` |
| Writes go to Master | ✅ DONE | 27 writes via `$this->db->write/insert` |
| Reads go to Slave | ✅ DONE | 41 reads via `$this->db->read/readOne` |
| Classes and methods commented | ⚠️ PARTIAL | Most models have per-method docstrings. Spot check: some simple methods skip the comment. Not a blocker but could be tightened |

---

### 3.3 Constants & Flexibility

| Check | Status | Notes |
|---|---|---|
| Role IDs use constants | ✅ DONE | `ROLE_OWNER`, `ROLE_STAFF`, `ROLE_AUDITOR` in `config/constants.php` |
| DB port numbers use constants | ⚠️ PARTIAL | Ports are in `config/database.php` (one-place), not as named constants. CLAUDE.md suggests `DB_MASTER_PORT` / `DB_SLAVE_PORT` constants but those don't exist yet |
| No magic numbers in logic | ✅ DONE | Audit action strings (`ACTION_LOGIN`, etc.) and roles all constants; date formats constants |

**Minor fix:** add `define('DB_MASTER_PORT', 3306); define('DB_SLAVE_PORT', 3307);` to `constants.php` and reference from `database.php`.

---

## SECTION 4 — DOCUMENTATION & REPORT (10%)

### 4.1 System Overview

| Check | Status | Notes |
|---|---|---|
| Written system overview exists | ✅ DONE | `PROJECT_CONTEXT.md` §1 + `PROJECT_STRUCTURE_EXPLAINED.md` intro |
| Each module functionality described | ✅ DONE | `PROJECT_CONTEXT.md` §4 covers all 6 processes (A–F) |

---

### 4.2 Database Schema Documentation

| Check | Status | Notes |
|---|---|---|
| ERD included and accurate | ⚠️ PARTIAL | Text-based schema in `DATABASE_FULL_DOCUMENTATION.md` is accurate; no visual ERD file |
| Every table's columns explained | ✅ DONE | `DATABASE_FULL_DOCUMENTATION.md` Part 1 covers all 11 tables with "Why It Matters" column |
| Relationships explained | ✅ DONE | FK list in Part 3; prose in `PROJECT_STRUCTURE_EXPLAINED.md` |
| Master-slave explained in plain language | ✅ DONE | Head-office / branch analogy in `PROJECT_STRUCTURE_EXPLAINED.md` §C |
| All triggers documented | ✅ DONE | Part 2 of `DATABASE_FULL_DOCUMENTATION.md` + plain-English version in `PROJECT_STRUCTURE_EXPLAINED.md` §E |

---

### 4.3 Screenshots

| Check | Status | Notes |
|---|---|---|
| Login page | ❌ MISSING | No `screenshots/` folder exists anywhere in the project |
| Dashboard (Owner view) | ❌ MISSING | |
| Dashboard (Staff view) | ❌ MISSING | |
| Inventory page | ❌ MISSING | |
| Delivery POS page | ❌ MISSING | |
| Sales POS page | ❌ MISSING | |
| Expenses page | ❌ MISSING | |
| Reports page | ❌ MISSING | |
| Admin panel | ❌ MISSING | |
| Audit Log viewer | ❌ MISSING | |
| Locked record (read-only) | ❌ MISSING | |
| Owner unlock button | ❌ MISSING | |
| Captions explaining each | ❌ MISSING | |

**Recommendation:** create `docs/screenshots/` folder. Capture all 12 screenshots (~30 min work) and embed them in a new `docs/SCREENSHOTS.md` with captions. Easy win for 10% of the grade.

---

## SECTION 5 — DELIVERABLES COMPLETENESS (10%)

### 5.1 Source Code

| Check | Status | Notes |
|---|---|---|
| Complete source code present + organized | ✅ DONE | Full MVC, 8 controllers, 12 models, 20 views |
| All pages and modules functional | ✅ DONE | Smoke test this session: 11 owner pages + 5 staff pages return HTTP 200 |
| No broken links or missing includes | ✅ DONE | PHP lint clean across every file |

---

### 5.2 Database File

| Check | Status | Notes |
|---|---|---|
| `sql/schema.sql` complete + runnable | ✅ DONE | Creates all 11 tables with FK, indexes, seeds |
| `sql/triggers.sql` complete with all 28 triggers | ✅ DONE | All 28 definitions (7 original + 21 change-logging) match live DB |
| Full DB export (.sql) ready for submission | ✅ DONE | `sql/backups/chicken_deluxe_FULL_BACKUP.sql` — dumped 2026-04-20, 72 KB, includes all tables, data, routines, triggers |
| Export includes tables, data, triggers, constraints | ✅ DONE | Verified: 0 `Outlet_ID` references, 47 `Kiosk_ID` references, 21 `trg_log_*` triggers present |

---

### 5.3 Submission

| Check | Status | Notes |
|---|---|---|
| May 4–7 deadline — on track? | ⚠️ PARTIAL | Code is ~90% rubric-ready, but trigger gap and missing screenshots/ERD block a top grade. ~2 weeks buffer is enough if attacked now |
| All files organized and ready to zip | ✅ DONE | Clean repo, no loose scratch files |

---

# 📊 SUMMARY

## Totals

- ✅ **DONE:** 65 items
- ⚠️ **PARTIAL:** 9 items
- ❌ **MISSING:** 11 items
- **Total items audited:** 85

## Readiness estimate per rubric section

| Section | Weight | Ready % | Points estimate (out of weight) |
|---|---|---|---|
| §1 Functionality & Implementation | 30% | **90%** | ~27/30 |
| §2 Database Design & Architecture | 35% | **82%** | ~29/35 |
| §3 Code Quality & Organization | 15% | **95%** | ~14/15 |
| §4 Documentation & Report | 10% | **55%** | ~6/10 |
| §5 Deliverables Completeness | 10% | **95%** | ~9.5/10 |
| **OVERALL** | **100%** | **~85%** | **~85/100** |

**Progress since last audit (2026-04-20):** the three biggest gaps — change-logging triggers, subqueries, and the stale DB backup — are now closed. Remaining upside is mostly documentation polish: a visual ERD and a handful of screenshots would comfortably push this into the **90–95%** range.

---

## 🏆 TOP 5 PRIORITY ITEMS

### 1. ~~Build a proper change-logging trigger system~~ ✅ DONE (2026-04-20)

**Status:** Completed. 21 new `trg_log_*` triggers plus 4 new Audit_Log columns. Verified live.

**Original plan (archived):** build AFTER INSERT/UPDATE/DELETE triggers + audit schema. Executed on 2026-04-20 as described below.

What to build:
- New `Change_Log` table with columns: `operation_type ENUM('INSERT','UPDATE','DELETE')`, `affected_table VARCHAR`, `record_id INT`, `old_values JSON`, `new_values JSON`, `changed_by_user INT`, `timestamp DATETIME`
- 12 new triggers: AFTER INSERT, AFTER UPDATE, AFTER DELETE on each of Sales, Inventory_Snapshot, Delivery, Expenses (4 tables × 3 events = 12)
- Each trigger inserts a row with the OLD and NEW values serialized as JSON
- Keep existing Audit_Log for application-level actions (login/logout/manual lock) — the two tables serve different purposes

### 2. Generate a visual ERD

**Impact:** §2.1 + §4.2 — up to 5 percentage points
**Effort:** 30 minutes

Options:
- MySQL Workbench → "Reverse Engineer" on the live DB → export as PNG
- Or dbdiagram.io (paste schema.sql, export image)
- Save as `docs/ERD.png` and embed in `DATABASE_FULL_DOCUMENTATION.md`

### 3. Capture all 12 screenshots with captions

**Impact:** §4.3 — up to 10 percentage points
**Effort:** 30–45 minutes

- Create `docs/screenshots/` folder
- Log in as owner, screenshot each page listed in §4.3
- Log in as staff, screenshot Dashboard + one locked record
- Write `docs/SCREENSHOTS.md` with a caption under each image explaining what the reader is seeing

### 4. ~~Add subqueries to ReportModel~~ ✅ DONE (2026-04-20)

`getProductsAboveAverageSales()` + `getKiosksWithoutEndingSnapshot()` added and tested.

Still on the wish-list (optional, low impact): admin-only hard-delete methods on User/Product/Kiosk for grader-literalism.

### 5. ~~Fresh DB dump~~ ✅ DONE (2026-04-20) — small config polish still pending

`sql/backups/chicken_deluxe_FULL_BACKUP.sql` created with `--routines --triggers`. Remaining 5-minute items:
- Add `DB_MASTER_PORT` / `DB_SLAVE_PORT` constants to `config/constants.php`
- Add `read_only=1` to slave `my.ini` and restart slave

---

## What's Already Excellent (don't change)

These areas are grader-ready and should not be touched:

- **Master-Slave replication** is live, zero lag, and correctly routed at the application level (§1.2, §2.3)
- **MVC architecture + code style** are textbook (§3.1, §3.2)
- **Constants and auth checks** are consistent everywhere (§3.2, §3.3)
- **Business-rule triggers** (lock protection, line-total calc) are well-designed (§1.3 partial credit)
- **Documentation files** (`CLAUDE.md`, `PROJECT_CONTEXT.md`, `DATABASE_FULL_DOCUMENTATION.md`, `PROJECT_STRUCTURE_EXPLAINED.md`) are thorough and consistent (§4.1, §4.2)

---

*PROJECT_CHECKLIST.md — Chicken Deluxe Inventory & Sales Monitoring System — Group 7 — BSIT 2-B — 2026*
